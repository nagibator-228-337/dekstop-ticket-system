<Window x:Class="DTS.MainPages.TicketView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ticket"
        Width="820"
        Height="520"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Border BorderBrush="{StaticResource DividerBrush}"
            BorderThickness="1"
            CornerRadius="10"
            Background="{StaticResource BackgroundBrush}">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TOP BAR -->
            <Grid Grid.Row="0"
                  Background="{StaticResource SurfaceBrush}"
                  MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="← Back"
                        Style="{StaticResource GhostButton}"
                        Click="CloseWindow_Click"
                        Margin="8"/>

                <TextBlock x:Name="ChatHeader"
                           Grid.Column="1"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Style="{StaticResource SubtitleText}"/>
            </Grid>

            <!-- MAIN -->
            <Grid Grid.Row="1" Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT PANEL -->
                <Border Grid.Column="0"
                        Style="{StaticResource CardStyle}"
                        Margin="0,0,8,0">

                    <Grid>

                        <!-- CLIENT VIEW -->
                        <Grid x:Name="ForClient" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBox Text="{Binding Subject}"
                                     IsReadOnly="True"
                                     Style="{StaticResource BaseTextBox}"/>

                            <TextBox Grid.Row="1"
                                     Text="{Binding Description}"
                                     IsReadOnly="True"
                                     Style="{StaticResource BaseTextBox}"/>
                        </Grid>

                        <!-- EMPLOYEE VIEW -->
                        <Grid x:Name="ForEmployee" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Row="0">
                                <TextBlock Text="Assign employee:"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource BodyText}"/>
                                <ComboBox x:Name="EmployeeComboBox"
                                          Width="160"
                                          Style="{StaticResource BaseComboBox}"/>

                                <Button Content="Pin"
                                        Style="{StaticResource SecondaryButton}"
                                        Click="PinButton_Click"
                                        MinHeight="25"
                                        MinWidth="50"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Grid.Row="1">
                                <TextBlock Text="                  Status:"
                                           VerticalAlignment="Center"
                                           Style="{StaticResource BodyText}"/>
                                <ComboBox x:Name="StatusComboBox"
                                          Width="160"
                                          Style="{StaticResource BaseComboBox}"
                                          ItemContainerStyle="{StaticResource {x:Type ComboBoxItem}}"/>
                            </StackPanel>

                            <TextBox Grid.Row="2"
                                     Text="{Binding Subject}"
                                     IsReadOnly="True"
                                     Style="{StaticResource BaseTextBox}"/>

                            <TextBox Grid.Row="3"
                                     Text="{Binding Description}"
                                     IsReadOnly="True"
                                     Style="{StaticResource BaseTextBox}"/>
                        </Grid>

                    </Grid>
                </Border>

                <!-- CHAT -->
                <Border Grid.Column="1"
                        Style="{StaticResource CardStyle}"
                        Margin="8,0,0,0">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <ScrollViewer>
                            <ItemsControl x:Name="MessageItemControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Padding="10"
                                                Margin="4"
                                                CornerRadius="10"
                                                MaxWidth="260">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#E5E7EB"/>
                                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                                    <Style.Triggers>
                                                        <!-- EMPLOYEE VIEW -->
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsEmployee}" Value="True"/>
                                                                <Condition Binding="{Binding AuthorType}" Value="Employee"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        </MultiDataTrigger>

                                                        <!-- CLIENT VIEW -->
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsEmployee}" Value="False"/>
                                                                <Condition Binding="{Binding AuthorType}" Value="Client"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <TextBlock Text="{Binding Text}"
                                                       TextWrapping="Wrap">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="Black"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Border}, Path=Background}" Value="{StaticResource PrimaryBrush}">
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <Grid Grid.Row="1" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="WritingMessageTextBox"
                                     Height="50"
                                     Style="{StaticResource BaseTextBox}"/>

                            <Button Grid.Column="1"
                                    Content="Send"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="SendButton_Click"
                                    Padding="16,8"
                                    MinHeight="25"
                                    MinWidth="45"/>    
                        </Grid>
                    </Grid>
                </Border>

            </Grid>
        </Grid>
    </Border>
</Window>
