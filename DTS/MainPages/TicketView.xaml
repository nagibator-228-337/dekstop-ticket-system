<Window x:Class="DTS.MainPages.TicketView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DTS.MainPages"
        mc:Ignorable="d"
        Title="Ticket: " Height="350" Width="650">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1" Margin="5" BorderBrush="Gray" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0"
                   Text="Chat with employee"
                   FontWeight="Bold"
                   Margin="10"
                   HorizontalAlignment="Center"/>

                <!-- Messages container -->
                <Border Grid.Row="1"
                Margin="10"
                BorderBrush="LightGray"
                BorderThickness="1"
                Background="#F9F9F9">
                </Border>

                <!-- Input panel -->
                <Grid Grid.Row="2" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                     Height="30"
                     VerticalAlignment="Center"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"/>

                    <Button Grid.Column="1"
                    Content="Send"
                    Margin="5,0,0,0"
                    Padding="10,5"/>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Column="0" Margin="5,5,10,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>

                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>

            <TextBox Name="SubjectTextBox"
                     Grid.Row="0"
                     IsReadOnly="True"
                     VerticalAlignment="Center"
                     />
            <TextBox Name="DescriptionTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     /> 

        </Grid>
    </Grid>
</Window>
