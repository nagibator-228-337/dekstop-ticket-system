<Window x:Class="DTS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DTS"
        Title="Ticket System"
        WindowState="Maximized"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="TopGrid" Grid.Row="0">
            <Button x:Name="Login"
                    Content="Login"
                    Style="{StaticResource GhostButton}"
                    Click="LoginButton_click"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="16,0"/>
        </Grid>

        
        <Grid x:Name="MainContentGrid" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="1300">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
    
            <!-- Buttons row -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Create Ticket Button -->
                <Button x:Name="Create"
                        Grid.Column="0"
                        Content="Create Ticket"
                        Style="{StaticResource LargePrimaryButton}"
                        Click="CreateButton_click"/>

                <!-- Search Ticket Button -->
                <Button x:Name="Search"
                        Grid.Column="2"
                        Content="Search Ticket"
                        Style="{StaticResource LargeSecondaryButton}"
                        Click="SearchButton_click"/>
            </Grid>
        </Grid>
        <Border Grid.Row="1" Background="#80000000" Visibility="Collapsed" Name="Drakening"/>

        <Grid Visibility="Visible" Grid.RowSpan="2">
            <Frame x:Name="MainFrame"
                   NavigationUIVisibility="Hidden"/>
        </Grid>

    </Grid>
</Window>
