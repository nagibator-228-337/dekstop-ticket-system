<UserControl x:Class="DTS.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="64"
             Background="{StaticResource SurfaceBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Left buttons / icons -->
            <ColumnDefinition Width="*"/>
            <!-- Center title -->
            <ColumnDefinition Width="Auto"/>
            <!-- Right buttons / actions -->
        </Grid.ColumnDefinitions>

        <!-- Left area: placeholder for future icons -->
        <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Margin="16,0">
        </StackPanel>

        <!-- Center title -->
        <TextBlock Grid.Column="1"
                   Text="🎫 DTS Ticket System"
                   Style="{StaticResource HeadlineText}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>


        <!-- Bottom divider -->
        <Border Height="1" Background="{StaticResource DividerBrush}" VerticalAlignment="Bottom" Grid.ColumnSpan="3"/>
        
    </Grid>
    
</UserControl>
